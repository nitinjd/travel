import express from 'express';
import cors from 'cors';
import dotenv from 'dotenv';
import bcrypt from 'bcryptjs';
import XLSX from 'xlsx';
import path from 'path';
import { fileURLToPath } from 'url';
import { pool,transaction } from './db.js';
import { requireAdmin,signAdmin } from './auth.js';
dotenv.config();
const app=express();app.use(cors());app.use(express.json({limit:'1mb'}));
const asyncRoute=fn=>(req,res,next)=>Promise.resolve(fn(req,res,next)).catch(next);
app.get('/api/health',asyncRoute(async(req,res)=>{await pool.query('SELECT 1');res.json({status:'ok'})}));
app.post('/api/admin/login',asyncRoute(async(req,res)=>{const{email,password}=req.body;const[[admin]]=await pool.query('SELECT id,email,password_hash,is_active FROM admins WHERE email=?',[email]);if(!admin||!admin.is_active||!await bcrypt.compare(password,admin.password_hash))return res.status(401).json({message:'Invalid email or password'});res.json({token:signAdmin(admin),admin:{id:admin.id,email:admin.email}})}));
app.post('/api/admin/bootstrap',asyncRoute(async(req,res)=>{const[[{count}]]=await pool.query('SELECT COUNT(*) count FROM admins');if(count)return res.status(409).json({message:'Admin already exists'});if(req.body.email!==process.env.ADMIN_EMAIL||req.body.password!==process.env.ADMIN_PASSWORD)return res.status(403).json({message:'Bootstrap credentials do not match environment'});const hash=await bcrypt.hash(req.body.password,12);const[result]=await pool.query('INSERT INTO admins(email,password_hash) VALUES(?,?)',[req.body.email,hash]);res.status(201).json({id:result.insertId})}));
app.get('/api/tours/active',asyncRoute(async(req,res)=>{const[[tour]]=await pool.query("SELECT * FROM tours WHERE status='ACTIVE' ORDER BY start_date LIMIT 1");if(!tour)return res.status(404).json({message:'No active tour'});const[travelOptions,roomTypes,itinerary]=await Promise.all([pool.query('SELECT * FROM travel_options WHERE tour_id=? AND is_active=1 ORDER BY sort_order,id',[tour.id]),pool.query('SELECT * FROM room_types WHERE tour_id=? AND is_active=1 ORDER BY sort_order,id',[tour.id]),pool.query('SELECT * FROM itinerary_items WHERE tour_id=? ORDER BY day_number,start_time,id',[tour.id])]);res.json({...tour,travelOptions:travelOptions[0],roomTypes:roomTypes[0],itinerary:itinerary[0]})}));
app.get('/api/admin/tours',requireAdmin,asyncRoute(async(req,res)=>{const[rows]=await pool.query('SELECT * FROM tours ORDER BY start_date DESC');res.json(rows)}));
app.post('/api/admin/tours',requireAdmin,asyncRoute(async(req,res)=>{const t=req.body;const[result]=await pool.query('INSERT INTO tours(name,location,start_date,end_date,food_charge_per_person,estimated_misc_expense,status) VALUES(?,?,?,?,?,?,?)',[t.name,t.location,t.start_date,t.end_date,t.food_charge_per_person||0,t.estimated_misc_expense||0,t.status||'DRAFT']);res.status(201).json({id:result.insertId})}));
app.put('/api/admin/tours/:id',requireAdmin,asyncRoute(async(req,res)=>{const t=req.body;await pool.query('UPDATE tours SET name=?,location=?,start_date=?,end_date=?,food_charge_per_person=?,estimated_misc_expense=?,status=? WHERE id=?',[t.name,t.location,t.start_date,t.end_date,t.food_charge_per_person||0,t.estimated_misc_expense||0,t.status,req.params.id]);res.json({success:true})}));
app.get('/api/admin/tours/:id/config',requireAdmin,asyncRoute(async(req,res)=>{const id=req.params.id;const[[tour]]=await pool.query('SELECT * FROM tours WHERE id=?',[id]);const[travelOptions,roomTypes,itinerary]=await Promise.all([pool.query('SELECT * FROM travel_options WHERE tour_id=? ORDER BY sort_order,id',[id]),pool.query('SELECT * FROM room_types WHERE tour_id=? ORDER BY sort_order,id',[id]),pool.query('SELECT * FROM itinerary_items WHERE tour_id=? ORDER BY day_number,start_time,id',[id])]);res.json({tour,travelOptions:travelOptions[0],roomTypes:roomTypes[0],itinerary:itinerary[0]})}));
app.post('/api/admin/travel-options',requireAdmin,asyncRoute(async(req,res)=>{const x=req.body;const[r]=await pool.query('INSERT INTO travel_options(tour_id,name,mode,charge_type,charge_amount,capacity,is_ac,is_active,sort_order) VALUES(?,?,?,?,?,?,?,?,?)',[x.tour_id,x.name,x.mode,x.charge_type,x.charge_amount||0,x.capacity||null,x.is_ac?1:0,x.is_active!==false?1:0,x.sort_order||0]);res.status(201).json({id:r.insertId})}));
app.put('/api/admin/travel-options/:id',requireAdmin,asyncRoute(async(req,res)=>{const x=req.body;await pool.query('UPDATE travel_options SET name=?,mode=?,charge_type=?,charge_amount=?,capacity=?,is_ac=?,is_active=?,sort_order=? WHERE id=?',[x.name,x.mode,x.charge_type,x.charge_amount||0,x.capacity||null,x.is_ac?1:0,x.is_active?1:0,x.sort_order||0,req.params.id]);res.json({success:true})}));
app.post('/api/admin/room-types',requireAdmin,asyncRoute(async(req,res)=>{const x=req.body;const[r]=await pool.query('INSERT INTO room_types(tour_id,name,charge_type,charge_amount,capacity,is_ac,extra_bed_allowed,extra_bed_charge,max_extra_beds,is_active,sort_order) VALUES(?,?,?,?,?,?,?,?,?,?,?)',[x.tour_id,x.name,x.charge_type,x.charge_amount,x.capacity,x.is_ac?1:0,x.extra_bed_allowed?1:0,x.extra_bed_charge||0,x.max_extra_beds||0,x.is_active!==false?1:0,x.sort_order||0]);res.status(201).json({id:r.insertId})}));
app.put('/api/admin/room-types/:id',requireAdmin,asyncRoute(async(req,res)=>{const x=req.body;await pool.query('UPDATE room_types SET name=?,charge_type=?,charge_amount=?,capacity=?,is_ac=?,extra_bed_allowed=?,extra_bed_charge=?,max_extra_beds=?,is_active=?,sort_order=? WHERE id=?',[x.name,x.charge_type,x.charge_amount,x.capacity,x.is_ac?1:0,x.extra_bed_allowed?1:0,x.extra_bed_charge||0,x.max_extra_beds||0,x.is_active?1:0,x.sort_order||0,req.params.id]);res.json({success:true})}));
app.post('/api/admin/itinerary',requireAdmin,asyncRoute(async(req,res)=>{const x=req.body;const[r]=await pool.query('INSERT INTO itinerary_items(tour_id,day_number,title,location,start_time,end_time,notes) VALUES(?,?,?,?,?,?,?)',[x.tour_id,x.day_number,x.title,x.location,x.start_time||null,x.end_time||null,x.notes||null]);res.status(201).json({id:r.insertId})}));
app.delete('/api/admin/:entity/:id',requireAdmin,asyncRoute(async(req,res)=>{const tables={travel:'travel_options',room:'room_types',itinerary:'itinerary_items'};const table=tables[req.params.entity];if(!table)return res.status(400).json({message:'Invalid entity'});await pool.query(`DELETE FROM ${table} WHERE id=?`,[req.params.id]);res.json({success:true})}));
app.post('/api/registrations/quote',asyncRoute(async(req,res)=>res.json(await calculateQuote(req.body))));
async function calculateQuote(body,connection=pool){const count=body.passengers?.length||0;if(!count)throw Object.assign(new Error('At least one passenger is required'),{status:400});const[[tour]]=await connection.query("SELECT * FROM tours WHERE id=? AND status='ACTIVE'",[body.tour_id]);const[[travel]]=await connection.query('SELECT * FROM travel_options WHERE id=? AND tour_id=? AND is_active=1',[body.travel_option_id,body.tour_id]);const[[room]]=await connection.query('SELECT * FROM room_types WHERE id=? AND tour_id=? AND is_active=1',[body.room_type_id,body.tour_id]);if(!tour||!travel||!room)throw Object.assign(new Error('Invalid tour selection'),{status:400});const units=Math.max(1,Number(body.room_units||1)),extraBeds=Math.max(0,Number(body.extra_beds||0));const food=count*Number(tour.food_charge_per_person);const travelAmount=travel.charge_type==='PER_PERSON'?count*Number(travel.charge_amount):Number(travel.charge_amount);const accommodation=units*Number(room.charge_amount)+extraBeds*Number(room.extra_bed_charge);return{passenger_count:count,food_amount:food,travel_amount:travelAmount,accommodation_amount:accommodation,total_amount:food+travelAmount+accommodation}}
app.post('/api/registrations',asyncRoute(async(req,res)=>{const id=await transaction(async c=>{const q=await calculateQuote(req.body,c);const b=req.body;const[[usage]]=await c.query("SELECT COUNT(p.id) used FROM registrations r JOIN passengers p ON p.registration_id=r.id WHERE r.tour_id=? AND r.travel_option_id=? AND r.status<>'CANCELLED'",[b.tour_id,b.travel_option_id]);const[[travel]]=await c.query('SELECT capacity FROM travel_options WHERE id=?',[b.travel_option_id]);if(travel.capacity&&Number(usage.used)+q.passenger_count>travel.capacity)throw Object.assign(new Error('Selected bus does not have enough seats'),{status:409});const[r]=await c.query('INSERT INTO registrations(tour_id,family_name,contact_name,contact_phone,contact_email,travel_option_id,room_type_id,room_units,extra_beds,food_amount,travel_amount,accommodation_amount,total_amount,status) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,\'SUBMITTED\')',[b.tour_id,b.family_name,b.contact_name,b.contact_phone,b.contact_email||null,b.travel_option_id,b.room_type_id,b.room_units,b.extra_beds||0,q.food_amount,q.travel_amount,q.accommodation_amount,q.total_amount]);for(const p of b.passengers)await c.query('INSERT INTO passengers(registration_id,name,gender,age) VALUES(?,?,?,?)',[r.insertId,p.name,p.gender,p.age]);return r.insertId});const[[registration]]=await pool.query('SELECT * FROM registrations WHERE id=?',[id]);res.status(201).json(registration)}));
const reportQuery=`SELECT r.id,r.family_name,r.contact_name,r.contact_phone,r.room_number,r.floor_number,r.room_units,r.extra_beds,r.food_amount,r.travel_amount,r.accommodation_amount,r.total_amount,r.status,t.name tour_name,t.location,vo.name travel_mode,vo.mode travel_mode_type,rt.name room_type,COUNT(p.id) member_count,GROUP_CONCAT(CONCAT(p.name,' (',p.gender,', ',p.age,')') ORDER BY p.id SEPARATOR ', ') members FROM registrations r JOIN tours t ON t.id=r.tour_id JOIN travel_options vo ON vo.id=r.travel_option_id JOIN room_types rt ON rt.id=r.room_type_id JOIN passengers p ON p.registration_id=r.id WHERE r.tour_id=? GROUP BY r.id ORDER BY vo.name,r.family_name`;
async function getReport(tourId,type){const[rows]=await pool.query(reportQuery,[tourId]);if(type==='bus')return rows.filter(x=>x.travel_mode_type==='BUS');if(type==='self')return rows.filter(x=>x.travel_mode_type==='SELF');return rows}
app.get('/api/admin/reports/:tourId',requireAdmin,asyncRoute(async(req,res)=>res.json(await getReport(req.params.tourId,req.query.type||'overall'))));
app.patch('/api/admin/registrations/:id/room',requireAdmin,asyncRoute(async(req,res)=>{await pool.query('UPDATE registrations SET room_number=?,floor_number=? WHERE id=?',[req.body.room_number||null,req.body.floor_number||null,req.params.id]);res.json({success:true})}));
app.get('/api/admin/reports/:tourId/excel',requireAdmin,asyncRoute(async(req,res)=>{const rows=await getReport(req.params.tourId,req.query.type||'overall');const data=rows.map(x=>({'Family':x.family_name,'Contact':x.contact_name,'Phone':x.contact_phone,'Members':x.member_count,'Member Details':x.members,'Travel Mode':x.travel_mode,'Room Type':x.room_type,'Rooms/Beds':x.room_units,'Extra Beds':x.extra_beds,'Room No.':x.room_number||'','Floor':x.floor_number||'','Food Amount':x.food_amount,'Travel Amount':x.travel_amount,'Accommodation Amount':x.accommodation_amount,'Family Total':x.total_amount}));const ws=XLSX.utils.json_to_sheet(data),wb=XLSX.utils.book_new();XLSX.utils.book_append_sheet(wb,ws,'Tour Report');const buffer=XLSX.write(wb,{type:'buffer',bookType:'xlsx'});res.setHeader('Content-Disposition',`attachment; filename=tour-${req.query.type||'overall'}-report.xlsx`);res.type('application/vnd.openxmlformats-officedocument.spreadsheetml.sheet').send(buffer)}));
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../../client/dist');app.use(express.static(root));app.use((req,res,next)=>req.path.startsWith('/api/')?next():res.sendFile(path.join(root,'index.html')));
app.use((error,req,res,next)=>{console.error(error);res.status(error.status||500).json({message:error.message||'Internal server error'})});
app.listen(process.env.PORT||5000,()=>console.log(`TourSetu running on port ${process.env.PORT||5000}`));
